// NotificationTrigger.cs
// Enum identifying the game events that can trigger a local notification.

namespace Evetero
{
    public enum NotificationTrigger
    {
        ResourceCapFull,
        LevelUpReached,
        GuildActivity,
        TimerComplete,
        DailyLogin
    }
}
